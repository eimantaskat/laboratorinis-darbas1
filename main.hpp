#pragma once

#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>
#include <fstream>
#include <sstream>
#include <regex>
#include <chrono>
#include <random>
#include <iomanip>

using std::string;
using std::cout;
using std::vector;
using std::ifstream;
using std::cin;
using hrClock = std::chrono::high_resolution_clock;

// #define std::chrono::duration_cast<std::chrono::nanoseconds> ns